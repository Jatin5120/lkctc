import 'package:flutter/material.dart';

class ClassAssignmentView extends StatelessWidget {
  const ClassAssignmentView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Assignments'));
  }
}
