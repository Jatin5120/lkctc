enum ButtonType {
  primary,
  secondary,
  outlined,
}

enum ButtonSize {
  small,
  medium,
  large,
}

enum UserType {
  admin,
  faculty,
  student,
}

enum DialogType {
  normal,
  success,
  warning,
  error,
}
